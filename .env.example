# Server-side AI configuration. Never commit a real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
PORT=80

# Server-only Gemini Live API key (never expose in browser config)
GEMINI_API_KEY=
GEMINI_LIVE_MODEL=gemini-3.8-live
